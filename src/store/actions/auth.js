import {
	LOGIN_SUCCESS,
	LOGIN_FAIL,
	REGISTER_SUCCESS,
	REGISTER_FAIL,
	GET_USER_SUCCESS,
	GET_USER_FAIL,
	LOGOUT_SUCCESS,
	LOGOUT_FAIL
} from '../actions/types';

export const getUserAction = (loggedIn) => dispatch=>{

}

export const loginAction = (data) => dispatch=>{

}

export const registserActoin = (data) => dispatch=>{

}

export const logoutAction = () => dispatch=>{
  
}
