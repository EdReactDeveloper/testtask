import React from 'react';
import Authorized from './Authorized';
import UnAuthorized from './UnAuthorized';

const Auth = ( ) => {

  return (
    <div>
     
    </div>
  );
};

export default Auth;
